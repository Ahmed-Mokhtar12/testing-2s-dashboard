# Database objects touched — 2026-09-01

Project: `yczcebfaqerlwfalrbjn` (shared by production and testing). All migration files are under `supabase/migrations/`; each has a `*_rollback.sql` sibling in the same commit.

## Tables where RLS was enabled

| Table | Change | Commit | Migration |
|---|---|---|---|
| `public.qms_request_log` | `ENABLE ROW LEVEL SECURITY`; `REVOKE ALL FROM anon, authenticated` | `d453f57` | `20260901100000_lockdown_rls_off_tables.sql` |
| `public.integrity_check_history` | `ENABLE ROW LEVEL SECURITY`; `REVOKE ALL FROM anon, authenticated` | `d453f57` | `20260901100000_lockdown_rls_off_tables.sql` |
| `public.n8n_chat_histories_backup_serafix_20260824` | `ENABLE ROW LEVEL SECURITY`; `REVOKE ALL FROM anon, authenticated` | `d453f57` | `20260901100000_lockdown_rls_off_tables.sql` |

## Functions where anon EXECUTE was revoked

| Function | Change | Commit | Migration |
|---|---|---|---|
| `public.has_role(uuid, public.app_role)` | `REVOKE EXECUTE FROM PUBLIC, anon`; `GRANT EXECUTE TO authenticated, service_role` | `667cea4` | `20260901100100_revoke_anon_role_oracles.sql` |
| `public.is_hotel_staff(uuid)` | `REVOKE EXECUTE FROM PUBLIC, anon`; `GRANT EXECUTE TO authenticated, service_role` | `667cea4` | `20260901100100_revoke_anon_role_oracles.sql` |
| `public.is_conversation_human_controlled(text)` | `REVOKE EXECUTE FROM PUBLIC, anon` | `b7d6634` (file, 2026-08-31); applied 2026-09-01 under `667cea4` | `20260831120100_revoke_anon_control_status.sql` |

## Policies dropped

| Policy | Table | Commit | Migration |
|---|---|---|---|
| `Hotel staff can update Chat History` | `public."Chat History"` | `7b90b95` | `20260901100300_chat_history_update_policy_and_freeze.sql` |
| `authenticated can read the sharepoint mirror` | `public.sharepoint_mirror` | `c70e49f` | `20260901100400_scope_mirror_and_sera_chat_policies.sql` |
| `Hotel staff can read website_chats` | `public."2s-dashboard_AI_Chat"` | `c70e49f` | `20260901100400_scope_mirror_and_sera_chat_policies.sql` |
| `users read their own sera chats; admins read all` | `public."2s-dashboard_AI_Chat"` | `c70e49f` | `20260901100401_sera_chat_policy_requires_staff.sql` |

## Policies created

| Policy | Table | Definition | Commit | Migration |
|---|---|---|---|---|
| `staff can read the sharepoint mirror` | `public.sharepoint_mirror` | `FOR SELECT TO authenticated USING (is_hotel_staff(auth.uid()))` | `c70e49f` | `20260901100400_scope_mirror_and_sera_chat_policies.sql` |
| `users read their own sera chats; admins read all` | `public."2s-dashboard_AI_Chat"` | `FOR SELECT TO authenticated USING (user_id = auth.uid() OR has_role(auth.uid(), 'admin'))` — superseded and dropped by `20260901100401` in the same commit | `c70e49f` | `20260901100400_scope_mirror_and_sera_chat_policies.sql` |
| `staff read their own sera chats; admins read all` | `public."2s-dashboard_AI_Chat"` | `FOR SELECT TO authenticated USING (is_hotel_staff(auth.uid()) AND (user_id = auth.uid() OR has_role(auth.uid(), 'admin')))` | `c70e49f` | `20260901100401_sera_chat_policy_requires_staff.sql` |

## Triggers modified

| Trigger | Table | Change | Commit | Migration |
|---|---|---|---|---|
| `chat_history_freeze_handled_by` (`BEFORE UPDATE`) | `public."Chat History"` | Trigger definition unchanged; its function `public.chat_history_freeze_handled_by()` replaced via `CREATE OR REPLACE` — derives `handled_by` on UPDATE only when `human_reply`, `"Ai Reply"` or `"Sender Message"` changed | `7b90b95` | `20260901100300_chat_history_update_policy_and_freeze.sql` |

Not touched: `chat_history_stamp_handled_by` (`BEFORE INSERT`) and `public.chat_history_stamp_handled_by()`.

## Cron jobs scheduled / rescheduled

| Job | Change | Commit | Migration |
|---|---|---|---|
| `rls-sentinel-daily` | New job: `cron.schedule('rls-sentinel-daily', '30 3 * * *', 'select public.enforce_rls_on_public_tables();')`, runs as `postgres` | `1a23769` | `20260901100200_schedule_rls_sentinel.sql` |

Not touched: `cron.job` entries other than `rls-sentinel-daily`.
