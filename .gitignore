# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.superpowers/
supabase/.temp/
test-results/
dist-test
dist-new
# What deploy-frontend.sh actually names its staging tree. It is removed on a
# successful deploy, so this only matters when one fails part-way — which is
# exactly when nobody wants 4 MB of untracked files in git status.
dist-staging
dist.bak-*
dist.old-*
dist.failed-*
